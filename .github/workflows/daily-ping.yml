name: Daily render ping

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:  # Allows manual triggering

jobs:
  curl-website:
    runs-on: ubuntu-latest

    steps:
      - name: Run cURL on Website
        run: curl -I https://finanalytica.onrender.com
